const productId = document.querySelector(".elasticity-main").dataset.productId;
const observations = document.querySelector("#observations");
const result = document.querySelector("#elasticity-result");
const marketResult = document.querySelector("#market-comparison");
const money = value => new Intl.NumberFormat("fa-IR").format(Math.round(value || 0));
const escapeHtml = value => String(value ?? "").replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;", "'":"&#039;"}[c]));
function addRow(point = {}) {
  const row = document.createElement("div");
  row.className = "observation-row";
  row.innerHTML = `<input name="period" placeholder="مثلاً ۱۴۰۴-۰۱" value="${escapeHtml(point.period || "")}" required><input name="price" type="number" min="1" placeholder="قیمت" value="${point.price || ""}" required><input name="units" type="number" min="0" placeholder="فروش" value="${point.units ?? ""}" required><button type="button" class="remove-row" aria-label="حذف">×</button>`;
  row.querySelector(".remove-row").onclick = () => { if (observations.children.length > 1) row.remove(); };
  observations.append(row);
}
function renderAnalysis(data) {
  const a = data.analysis;
  if (a.status !== "ready") { result.innerHTML = `<div class="result-empty"><strong>داده کافی نیست</strong><span>${escapeHtml(a.message)}</span><small>${a.sample_size} رکورد، ${a.distinct_prices} سطح قیمت</small></div>`; return; }
  const sign = a.elasticity < 0 ? "" : "+";
  const rows = a.scenarios.map(s => `<tr class="${s.price === a.recommended_price ? "best" : ""}"><td>${money(s.price)} تومان</td><td>${money(s.demand_units)} واحد</td><td>${s.demand_change_percent > 0 ? "+" : ""}${s.demand_change_percent}%</td><td>${money(s.revenue)} تومان</td><td>${s.revenue_change_percent > 0 ? "+" : ""}${s.revenue_change_percent}%</td></tr>`).join("");
  result.innerHTML = `<div class="result-head"><div><small>کشش قیمتی برآوردشده</small><strong>${sign}${a.elasticity}</strong><span>به ازای ۱٪ تغییر قیمت، تقاضا حدود ${Math.abs(a.elasticity)}٪ ${a.elasticity < 0 ? "تغییر معکوس" : "تغییر"} می‌کند.</span></div><b>اطمینان ${a.confidence}%</b></div><div class="metric-line"><span>قیمت پیشنهادی برای بیشینه‌کردن درآمد در سناریوها</span><strong>${money(a.recommended_price)} تومان</strong></div><p class="result-message">${escapeHtml(a.interpretation)} ${escapeHtml(a.message)}</p><div class="table-wrap"><table><thead><tr><th>قیمت</th><th>تقاضای پیش‌بینی‌شده</th><th>تغییر تقاضا</th><th>درآمد</th><th>تغییر درآمد</th></tr></thead><tbody>${rows}</tbody></table></div><small class="result-meta">${a.sample_size} رکورد · ${a.distinct_prices} سطح قیمت · R²=${a.r_squared}</small>`;
}
function listingCards(items) { return items.length ? `<div class="listing-grid">${items.map(item => `<a class="listing-card" href="${escapeHtml(item.url)}" target="_blank" rel="noopener noreferrer"><span>${escapeHtml(item.source)}</span><strong>${escapeHtml(item.title)}</strong><b>${money(item.price)} تومان</b>${item.native_price ? `<small>${money(item.native_price)} ${escapeHtml(item.native_currency)}</small>` : ""}</a>`).join("")}</div>` : `<div class="market-empty">نتیجه‌ای برای این گروه پیدا نشد.</div>`; }
function renderMarket(data) { const internal = data.internal_listings || []; const foreign = data.foreign_listings || []; const historical = data.historical_elasticity; const historicalCopy = historical ? `ضریب تاریخی عبور نرخ ارز: ${historical.pass_through}٪ با هر ۱٪ تغییر در قیمت معادل خارجی · اطمینان آماری R²=${historical.r_squared}` : "برای ضریب تاریخی حداقل ۳ snapshot در ۲ سطح قیمت متفاوت لازم است."; marketResult.innerHTML = `<div class="market-head"><div><small>مقایسه قیمت بازار</small><h2>داخلی در برابر خارجی</h2><p>لینک‌ها از همان نتایج جست‌وجوی بازار دریافت شده‌اند.</p></div><b>${data.sample_size} نتیجه</b></div><div class="market-metrics"><div><small>میانه داخلی</small><strong>${data.internal_median ? money(data.internal_median) + " تومان" : "—"}</strong></div><div><small>میانه خارجی معادل تومان</small><strong>${data.foreign_median_toman ? money(data.foreign_median_toman) + " تومان" : "—"}</strong></div><div><small>قیمت فعلی / خارجی</small><strong>${data.current_to_foreign_ratio ? data.current_to_foreign_ratio + " برابر" : "—"}</strong></div></div><div class="fx-box"><strong>اثر تغییر دلار روی قیمت داخلی</strong><span>${escapeHtml(historicalCopy)}</span><small>تا آماده‌شدن تاریخچه، سناریوها با فرض انتقال ۱۰۰٪ تغییر نرخ ارز نمایش داده می‌شوند.</small><div class="fx-table">${(data.scenarios || []).map(s => `<div><b>${s.fx_change_percent > 0 ? "+" : ""}${s.fx_change_percent}% ارز</b><span>قیمت خارجی: ${s.foreign_equivalent_price ? money(s.foreign_equivalent_price) : "—"}</span><strong>قیمت داخلی سناریو: ${s.internal_price_scenario ? money(s.internal_price_scenario) : "—"}</strong></div>`).join("")}</div></div><div class="listing-section"><h3>محصولات داخلی</h3>${listingCards(internal)}</div><div class="listing-section"><h3>محصولات خارجی</h3>${listingCards(foreign)}</div>`; }
async function load() { const [response, marketResponse] = await Promise.all([fetch(`/api/merchant/products/${productId}/elasticity`), fetch(`/api/merchant/products/${productId}/market-comparison`)]); if (response.ok) { const data = await response.json(); renderAnalysis(data); (data.analysis.points || []).filter(p => p.source === "manual").forEach(addRow); } if (!observations.children.length) { addRow(); addRow(); addRow(); } if (marketResponse.ok) renderMarket(await marketResponse.json()); else marketResult.innerHTML = `<div class="result-empty">دریافت مقایسه بازار فعلاً ممکن نیست.</div>`; }
document.querySelector("#add-observation").onclick = () => addRow();
document.querySelector("#elasticity-form").onsubmit = async event => { event.preventDefault(); const rows = [...observations.children].map(row => Object.fromEntries([...row.querySelectorAll("input")].map(input => [input.name, input.name === "period" ? input.value : Number(input.value)]))); const response = await fetch(`/api/merchant/products/${productId}/elasticity`, { method: "POST", headers: {"Content-Type":"application/json"}, body: JSON.stringify({observations: rows}) }); const data = await response.json(); if (!response.ok) { result.innerHTML = `<div class="result-empty">${escapeHtml(data.detail || "ثبت داده ناموفق بود.")}</div>`; return; } renderAnalysis(data); };
load();
